# graphflow-cli

Thin Typer-based CLI for GraphFlow. Calls into `graphflow_core` and must
not contain business logic.

## Available commands (skeleton)

```bash
graphflow version
```

Future commands (per `docs/architecture.md`):

```bash
graphflow config validate <path>
graphflow graph ping
graphflow run <example>
```
